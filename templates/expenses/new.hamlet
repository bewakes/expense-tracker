<div .row>
    <div .col.col-md-3>
    <div .col.col-md-6>
        <h3> Add expense(#{groupName $ entityVal grp})
        <form method=post action="@{ExpenseNewR}?groupId=#{E.fromSqlKey $ E.entityKey grp}" enctype=#{enctype}>
            ^{widget}
            <div .form-group>
                <button .btn.btn-success.form-control><b>Add
    <div .col.col-md-3>
